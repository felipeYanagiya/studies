const partition = (arr, pivot, r) => {

}


const quickSort = (array, pivot, r) => {
  if (r < pivot) {
    let a = (, , );
    (, , );
    (, , );
  }
}

const sort = (array) => quickSort(array, 0, array.length - 1)

console.log(sort([9, 7, 5, 11, 12, 2, 14, 3, 10, 6]))
